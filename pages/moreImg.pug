extends layout/main
block content 
    main.container  
        block content
        h1 Mas imagenes
          .grid 
          
           if finalObject && finalObject.length> 0 
            each obj in finalObject
                each imgs in obj.moreImg
                    .card 
                        img(src=imgs)
                    